---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: federatedpriorityclasses.work.karmada.io
spec:
  group: work.karmada.io
  names:
    categories:
    - karmada-io
    kind: FederatedPriorityClass
    listKind: FederatedPriorityClassList
    plural: federatedpriorityclasses
    shortNames:
    - fpc
    singular: federatedpriorityclass
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: Value of federatedPriorityClass's Priority
      jsonPath: .value
      name: Value
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FederatedPriorityClass is the Schema for the federatedPriorityClass
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          description:
            description: |-
              description is an arbitrary string that usually provides guidelines on
              when this federatedPriorityClass should be used.
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          value:
            description: |-
              value represents the integer value of this federatedPriorityClass. This is the actual priority that workloads
              receive when jobs have the name of this class in their federatedPriorityClass label.
              Changing the value of federatedPriorityClass doesn't affect the priority of workloads that were already created.
            format: int32
            type: integer
        required:
        - value
        type: object
    served: true
    storage: true
    subresources: {}
